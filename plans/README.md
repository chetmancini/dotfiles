# Implementation Plans

Generated by the improve skill on 2026-08-07 for **dotfiles modernization**.
Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your row
when done.

**Planned against**: `a52b4b5` (`a52b4b571b2bc9670bb881008a3ab9a8018de5d7`)

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Consolidate version managers onto mise | P1 | M | — | TODO |
| 002 | Modularize `.zshrc` and drop oh-my-zsh | P1 | L | 001 recommended | TODO |
| 003 | 1Password-native secrets loading | P1 | M | — | TODO |
| 004 | Prune legacy surface + Brewfile profiles | P2 | M | 002 | TODO |
| 005 | Shell UX: atuin + direnv | P2 | M | 002 | TODO |
| 006 | `dot` CLI dispatcher for `bin/` | P2 | M | — | TODO |
| 007 | Agent / MCP config layout | P3 | M | 003 helpful | TODO |
| 008 | Documentation reconciliation pass | P2 | S | 001–007 best-effort | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (reason) | `REJECTED` (reason)

## Dependency notes

- **001 before 002** is recommended so modular zsh only wires mise once, not nvm/pyenv shims that will be deleted.
- **002 before 004**: pruning oh-my-zsh from install/doctor only makes sense after the shell no longer sources it.
- **002 before 005**: atuin/direnv hooks belong in the new `zsh/tools/` modules, not a 663-line monolith.
- **003 is independent** of shell modularization; can run in parallel with 001/002.
- **006 is independent**; can run anytime. Nice follow-on after 004 if `doctor` changes.
- **007** benefits from 003 if agent configs need secrets via `op` rather than plain env files.
- **008** should run last (or re-run after each major plan) so README/CLAUDE/docs match reality.

## Parallelism

Safe parallel tracks:

1. **Runtimes/shell**: 001 → 002 → 004 → 005
2. **Secrets**: 003 (alone or with 001)
3. **DX**: 006 (alone)
4. **Agents**: 007 after or with 003
5. **Docs**: 008 last

## Findings considered and rejected

- **Replace custom λ prompt with starship/oh-my-posh by default**: deferred. Prompt works; async/git lag is a follow-up if monorepo pain appears. Mentioned as optional in 002/005 only.
- **Delete vim/iTerm without operator confirmation**: plan 004 archives/removes only after explicit opt-in flags in the plan steps; default is “document as legacy / stop installing,” not hard-delete of personal data.
- **Force-remove AI casks from Brewfile**: split into optional profile instead of deleting the user’s intentional toolkit.
- **Rewrite all of `bin/` in Rust/Go**: not worth it; bash + `dot` dispatcher is enough.

## How to execute a plan

```bash
# From repo root
git checkout -b advisor/001-mise-consolidation   # use plan’s branch name
# Follow plans/001-….md step by step
make check
doctor
# Update this README status row when done
```

Do **not** push or open a PR unless the operator asks.
